interface:
  display_name: "Debug Agent"
  short_description: "Evidence-first root cause debugging"
  default_prompt: "Use $debug-agent to reproduce this bug, gather runtime evidence, identify the root cause, patch it, and verify the fix."
